{
  "name": "decoupled-blocks-reference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"astro dev --port 3200\"",
    "build": "tinacms build && astro build",
    "preview": "astro preview --port 3200"
  },
  "dependencies": {
    "astro": "^7.0.3",
    "tinacms": "^3.9.3"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.5.1"
  }
}
